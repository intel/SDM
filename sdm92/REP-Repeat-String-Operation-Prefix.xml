<instruction name="REP">
 <copyright>Copyright (C) Intel Corporation. All Rights Reserved.</copyright>
 <shortdesc>Repeat String Operation (Prefix)</shortdesc>
 <longdesc><para unique="1091973">
   <string>Repeats a string instruction the number of times specified in the count register. The count register is CX, ECX, or RCX, depending on the instruction&#8217;s address size. The REP (repeat) mnemonic is a prefix that can be added to the </string><indexterm><primary>LODS instruction;STOS instruction</primary></indexterm><indexterm><primary>INS instruction;OUTS instruction;MOVS instruction</primary></indexterm><string>INS, OUTS, MOVS, LODS, and STOS instructions.</string></para>
  <para unique="1091980">
   <string>The REP prefixes apply only to one string instruction at a time. To repeat a block of instructions, use the LOOP instruction or another looping construct. The REP prefixes causes the associated instruction to be repeated until the count in register is decremented to 0.</string></para>
  <para unique="1095848">
   <string>Each of the string instructions uses a source address, a destination address, or both. The source address is DS:SI, DS:ESI, or DS:RSI, depending on the instruction&#8217;s address size; the DS segment may be overridden by an instruc&#173;tion prefix. The destination address is ES:DI, ES:EDI, or ES:RDI, depending on the instruction's address size; the ES segment may not be overridden. (Note that, in 64-bit mode, the base addresses of the CS, DS, ES, and SS segments are treated as zero.)</string></para>
  <para unique="1095853">
   <string>Similarly, the size of the count register is the instruction&#8217;s address size. Thus, the default count register in 64-bit mode is RCX; REX.W has no effect on the address size and the count register. If 67H is used to override the default address size, the size of the count register is also overridden.</string></para>
  <para unique="1092026">
   <string>A repeating string operation can be suspended by an exception or interrupt. When this happens, the state of the registers is preserved to allow the string operation to be resumed upon a return from the exception or interrupt handler. The source and destination registers point to the next string elements to be operated on, the EIP register points to the string instruction, and the ECX register has the value it held following the last successful iteration of the instruction. This mechanism allows long string operations to proceed without affecting the interrupt response time of the system.</string></para>
  <para unique="1092028">
   <string>Use the REP INS and REP OUTS instructions with caution. Not all I/O ports can handle the rate at which these instructions execute. Note that a REP STOS instruction is the fastest way to initialize a large block of memory.</string></para>
  <para unique="1092030">
   <string>REP INS may read from the I/O port without writing to the memory location if an exception or VM exit occurs due to the write (e.g., #PF). If this would be problematic, for example because the I/O port read has side-effects, soft&#173;ware should ensure the write to the memory location does not cause an exception or VM exit.</string></para>
  </longdesc>
 <subsection title="Flags Affected">
  <para unique="1092054">
   <string>None.</string></para>
  </subsection>
 <subsection title="Exceptions (All Operating Modes)">
  <para unique="1092056">
   <string>Exceptions may be generated by an instruction associated with the prefix.</string></para>
  </subsection>
 </instruction>
