<category>
    <name>memory/linear</name>
    <shortdesc>Linear memory system</shortdesc>
    <longdesc>

        <para>
            The linear address space consists of the processor’s addressable memory space.
        </para>

        <para>
            Paging provides a mechanism for implementing a conventional
            demand-paged, virtual-memory system where sections of a program’s
            execution environment are mapped into physical memory as needed.

            Paging can also be used to provide isolation between multiple tasks.

            When operating in protected mode, some form of segmentation must be
            used.

            The use of paging is optional.
        </para>

        <para>
            If paging is not used, the linear address space of the processor is
            mapped directly into the physical address space of processor.
        </para>

    </longdesc>
</category>
